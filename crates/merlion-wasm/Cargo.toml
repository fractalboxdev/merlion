[package]
name = "merlion-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
merlion-render = { path = "../merlion-render" }

[features]
# Adds a `trap` export that executes `unreachable`, for the JS glue's recovery tests.
test-trap = []
